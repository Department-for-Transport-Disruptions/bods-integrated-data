{
    "name": "@bods-integrated-data/gtfs-rt-service-alerts-downloader",
    "version": "0.1.0",
    "description": "Download GTFS RT service alerts",
    "scripts": {
        "build:ci": "rm -rf ./dist && tsc && node ./esbuild.mjs && cd ./dist && zip -rq ./gtfs-rt-service-alerts-downloader.zip .",
        "build:local": "pnpm run build:ci && mkdir -p ../dist && cp ./dist/gtfs-rt-service-alerts-downloader.zip ../dist",
        "test": "vitest run"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "dependencies": {
        "@bods-integrated-data/shared": "workspace:^",
        "zod-validation-error": "^3.3.0"
    }
}
