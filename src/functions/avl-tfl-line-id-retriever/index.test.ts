import { describe, it } from "vitest";

describe("getLineIds", () => {
    it("should return an array of line IDs if API returns data", () => {

    });
    it("should throw an error if an empty response is returned from API", () => {
    });
    it("should throw an error if an error is return from the API", () => {
    });
});