{
    "name": "@bods-integrated-data/avl-feed-validator",
    "version": "0.1.0",
    "description": "AVL Feed Validator",
    "scripts": {
        "build:ci": "rm -rf ./dist && tsc && node ./esbuild.mjs && cd ./dist && zip -rq ./avl-feed-validator.zip .",
        "build:local": "pnpm run build:ci && mkdir -p ../dist && cp ./dist/avl-feed-validator.zip ../dist",
        "test": "vitest run"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "dependencies": {
        "@bods-integrated-data/shared": "workspace:^",
        "axios": "^1.9.0"
    },
    "devDependencies": {
        "mockdate": "^3.0.5"
    }
}
