{
    "name": "@bods-integrated-data/avl-mock-data-receiver",
    "version": "0.1.0",
    "description": "AVL mock data receiver",
    "scripts": {
        "build:ci": "rm -rf ./dist && tsc && node ./esbuild.mjs && cd ./dist && zip -rq ./avl-mock-data-receiver.zip .",
        "build:local": "pnpm run build:ci && mkdir -p ../dist && cp ./dist/avl-mock-data-receiver.zip ../dist",
        "test": "vitest run"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "dependencies": {
        "@bods-integrated-data/shared": "workspace:^"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.110",
        "@types/node": "^20.11.19"
    }
}
