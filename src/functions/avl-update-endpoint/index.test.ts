import { describe, it } from "vitest";

describe("avl-update-endpoint", () => {
    it("should unsubscribe from data producer and resubscribe with new details", () => {});
    it("should resubscribe with new details from data producer even if unsubscribe step is unsuccessful", () => {});
    it("should throw a 404 status code if given subscription ID does not exist in dynamo", () => {});
});
