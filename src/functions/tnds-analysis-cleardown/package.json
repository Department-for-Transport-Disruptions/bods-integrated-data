{
    "name": "@bods-integrated-data/tnds-analysis-cleardown",
    "version": "0.1.0",
    "description": "Clears down the TNDS analysis table",
    "scripts": {
        "build:ci": "rm -rf ./dist && tsc && node ./esbuild.mjs && cd ./dist && zip -rq ./tnds-analysis-cleardown.zip .",
        "build:local": "pnpm run build:ci && mkdir -p ../dist && cp ./dist/tnds-analysis-cleardown.zip ../dist",
        "test": "vitest run"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "dependencies": {
        "@bods-integrated-data/shared": "workspace:^",
        "zod": "3.23.8"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.110",
        "@types/node": "^20.11.19",
        "mockdate": "^3.0.5"
    }
}
