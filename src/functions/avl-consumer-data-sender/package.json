{
    "name": "@bods-integrated-data/avl-consumer-data-sender",
    "version": "0.1.0",
    "description": "Lambda for sending AVL subscription data to subscribers",
    "scripts": {
        "build:ci": "rm -rf ./dist && tsc && node ./esbuild.mjs && cd ./dist && zip -rq ./avl-consumer-data-sender.zip .",
        "build:local": "pnpm run build:ci && mkdir -p ../dist && cp ./dist/avl-consumer-data-sender.zip ../dist",
        "test": "vitest run"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "dependencies": {
        "@bods-integrated-data/shared": "workspace:^",
        "axios": "^1.9.0"
    },
    "devDependencies": {
        "mockdate": "^3.0.5"
    }
}
