{
    "name": "@bods-integrated-data/db-migrator",
    "version": "0.1.0",
    "description": "",
    "scripts": {
        "build:ci": "rm -rf ./dist && tsc && cp -rL node_modules ./dist && cp -rL ../../shared/node_modules/* ./dist/node_modules && cd ./dist && zip -rq ./db-migrator.zip .",
        "build:local": "pnpm run build:ci && mkdir -p ../dist && cp ./dist/db-migrator.zip ../dist/db-migrator.zip"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "dependencies": {
        "kysely": "^0.27.2",
        "lambda-log": "^3.1.0"
    },
    "devDependencies": {
        "@types/lambda-log": "^3.0.3",
        "@types/node": "^20.11.19"
    }
}
