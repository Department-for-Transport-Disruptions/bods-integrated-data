{
    "name": "@bods-integrated-data/avl-aggregate-siri-vm",
    "version": "0.1.0",
    "description": "This function is to regularly query the AVL database to get the most recent vehicle location for each unique vehicle in the database and aggregate it into a single SIRI-VM file.",
    "scripts": {
        "test": "vitest run",
        "lint": "eslint .",
        "build:ci": "rm -rf ./dist && tsc && node ./esbuild.mjs && cd ./dist && zip -rq ./avl-aggregate-siri-vm.zip .",
        "build:local": "pnpm run build:ci && mkdir -p ../dist && cp ./dist/avl-aggregate-siri-vm.zip ../dist/avl-aggregate-siri-vm.zip"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "dependencies": {
        "@baselime/lambda-logger": "^0.2.2",
        "@bods-integrated-data/shared": "workspace:^"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.110",
        "@types/node": "^20.11.19",
        "mockdate": "^3.0.5",
        "zod": "^3.22.4"
    }
}
