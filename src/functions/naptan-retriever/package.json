{
    "name": "@bods-integrated-data/naptan-retriever",
    "version": "0.1.0",
    "description": "",
    "scripts": {
        "build:ci": "rm -rf ./dist && tsc && cp -rL node_modules ./dist && cp -rL ../../shared/node_modules/* ./dist/node_modules && cd ./dist && zip -rq ./naptan-retriever.zip .",
        "build:local": "pnpm run build:ci && mkdir -p ../dist && cp ./dist/naptan-retriever.zip ../dist/naptan-retriever.zip"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "dependencies": {
        "@aws-sdk/client-s3": "^3.515.0",
        "axios": "^1.6.7",
        "lambda-log": "^3.1.0"
    },
    "devDependencies": {
        "@types/lambda-log": "^3.0.3",
        "@types/node": "^20.11.19"
    }
}
