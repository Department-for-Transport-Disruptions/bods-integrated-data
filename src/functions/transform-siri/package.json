{
    "name": "@bods-integrated-data/transform-siri",
    "version": "0.1.0",
    "description": "This function is to transform AVL SIRI-VM data into a CSV format.",
    "scripts": {
        "test": "vitest run",
        "build:ci": "rm -rf ./dist && tsc && node ./esbuild.mjs && cd ./dist && zip -rq ./transform-siri.zip .",
        "build:local": "pnpm run build:ci && mkdir -p ../dist && cp ./dist/transform-siri.zip ../dist/transform-siri.zip"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "dependencies": {
        "lambda-log": "^3.1.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.110",
        "@types/lambda-log": "^2.2.1",
        "@types/node": "^20.11.19",
        "@types/xml2js": "^0.4.14",
        "json-2-csv": "^5.0.1",
        "xml2js": "^0.6.2",
        "zod": "^3.22.4"
    }
}
